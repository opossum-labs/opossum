# Saving Options

Opossum provides new saving options for managing files generated during its workflow.

## Repository Handling

The general file repository can be deleted before starting an analysis. No leftover files are expected to remain after cleanup.

## Configuration

The selected saving options are stored in a configuration file. Once the configuration has been set, Opossum reuses the stored settings, and the user will not be asked for the saving options again.
